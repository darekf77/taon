
const path = require('path')
    var { config } = { config: {} };

    config = {

      domain: 'electron-remote-service.example.domain.com',
      useDomain: false,
      title: 'Electron Remote Service',
      pwa: {
        // start_url: ''
      }

    }
    module.exports = exports = { config };
    