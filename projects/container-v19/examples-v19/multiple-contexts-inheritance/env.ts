import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'multiple-contexts-inheritance.example.domain.com',
    title: 'Multiple Contexts Inheritance',
  },
};
export default env;
