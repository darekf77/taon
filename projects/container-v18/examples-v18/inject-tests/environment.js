
const path = require('path')
    var { config } = { config: {} };

    config = {

      domain: 'inject-tests.example.domain.com',
      useDomain: false,
      title: 'Inject Tests',
      pwa: {
        // start_url: ''
      }

    }
    module.exports = exports = { config };
    