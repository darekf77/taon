
const path = require('path')
    var { config } = { config: {} };

    config = {

      domain: 'circural-deps-ctx.example.domain.com',
      useDomain: false,
      title: 'Circural Deps Ctx',
      pwa: {
        // start_url: ''
      }

    }
    module.exports = exports = { config };
    