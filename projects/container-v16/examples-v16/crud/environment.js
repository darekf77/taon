
const path = require('path')
    var { config } = { config: {} };

    config = {

      domain: 'crud.example.domain.com',
      useDomain: false,
      title: 'Crud',
      pwa: {
        // start_url: ''
      }

    }
    module.exports = exports = { config };
    